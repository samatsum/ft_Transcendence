# TEAM_PLAN — 4人体制の作業分担（完了済み + 残作業）

**位置づけ**: これまで全工程を1人で進めてきたが、ここから4人体制へ移行する。
本書は「完了済み（Engine / Gameplay）」と「残作業（Backend / Frontend）」を一望し、
残りを **4人が並行で進められるよう役割に割る**ための正本。
個々の Issue の受入条件・依存は [BACKLOG.md](./5-バックログ.md) が正本。本書は**分担・依存の同期点・引き継ぎ資産・並行作業ルール**に集中する。

> 設計書は各レーンを **壱=Engine / 弐=Backend / 参=Frontend / 肆=Gameplay** と表記する（BACKLOG 等）。
> 本書は新メンバー向けに役割名で記す。
> 文中の ①〜⑤ は**設計書番号**（①ENGINE_SEPARATION / ②WS_PROTOCOL /
> ③REST_API / ④FRONTEND / ⑤BACKLOG）で、レーン番号ではない。

作成: 2026-07-19 ／ 更新: 2026-07-23（W-01 完了・CI 全ジョブ green 達成を反映。
**期間を 5 日間へ変更し §5.1 に日割りを追加**）

> **日程はここが正本です** → [§5.1 5日間の日割り](#51-5日間の日割り現在有効な日程の正本)。
> ⓪ 全体アーキテクチャ設計 §7 の 14 日表は当初計画（2週間）の記録で、日程としては無効です。

---

## 0. 全体像（なぜこの割り方になるか）

元の設計は **4レーン = 4人**（Engine / Backend・DevOps / Frontend / Gameplay）を想定していた。
それを1人で全部やってきた結果:

- **C エンジン側の2レーン（Engine E-01〜E-14 / Gameplay G-01〜G-10）は完了**。
  native/web/sim の3ターゲット、sim 公開 API、snapshot 受け口、
  対戦マップ4枚、CI、受入テスト（`make test` 85検査）まで揃っている。
- **残るは TypeScript の2レーン（Backend W-01〜W-16 / Frontend F-01〜F-12）だけ**。
  うち **W-01（リポジトリ骨格）は完了済み**で、3人のブロッカーは既に外れている（§3）。

そこで **残る2レーンをそれぞれ2人に割り、4人**とする。C の成果物は「4人が使う部品」として §2 に整理した。

> **重要**: 現在「遊べる」のは **1人 vs NPC** のみ。
> **対人戦（2ブラウザで 2vs2 RSP＝ゲート2）は、この残作業が初めて成立させる**。
> C 側は素材（複数席・入力源切替・snapshot 配信/受信）を用意し終えた段階。

---

## 1. 完了済み（Engine・Gameplay）— もう触らなくてよい

| レーン | 範囲 | 状態 | 参照 |
|---|---|---|---|
| Engine | E-01〜E-07（層分離・render.wasm・ゲート1） | ✅ | GATE1_REPORT |
| Engine | E-08〜E-12（web入力・memリーダ・sim API・sim.wasm・snapshot受け口） | ✅ | ENGINE_PHASE3_REPORT |
| Engine | E-13（描画ハードニング）/ E-14（CI） | ✅ | ENGINE_E13_E14_REPORT |
| Gameplay | G-01〜G-10（戦闘員統合・先取点・FPS 1vs1・敵ハザード・対戦マップ・BMP無効化） | ✅ | ENGINE_PHASE2/PHASE3_REPORT |

- **G-09 テストプレイ承認は完了**（§6）。テストプレイでプレイ感を samatsum が調整のうえ承認。
- **CI は 2026-07-23 に全ジョブ green を達成**（§6）。
  `native` / `wasm` / `web-app` の3ジョブが揃って success になり、§7-2 の「green ならマージ可」運用が有効になった。

---

## 2. 引き継ぎ資産（残作業が「使う」部品）

4人が新しく書く W/F は、下の**既にある部品へ配線するだけ**の箇所が多い。ここが一番の時短ポイント。

| 部品 | 実体 | 誰が使う | 使い方の正本 |
|---|---|---|---|
| **sim 公開 API** | `sim.wasm` + `codes/includes/platform/sim.h` | 担当2（W-10） | **ENGINE_PHASE3_REPORT §W-10 申し送り（9項目）**＝呼び出し順・席とチーム対応・id 照合・target_score・yaw 権威・1ルーム=1インスタンス |
| **snapshot レイアウト** | `platform/sim.h`（f64 フラット配列 header5+戦闘員9/体） | 担当2（配信）/ 担当4（受信） | 同上。`record.mjs` の `takeSnapshot()` が JSON 化の参照実装 |
| **クライアント描画** | `render.wasm` + `web_apply_snapshot(flat,len,view_id)` + `web_render_frame()` | 担当4（F-06） | ② §5-C。`web_init(map,is_rsp,w,h)` で内部解像度指定（E-13） |
| **補間** | `web/snapshot_interp.js`（位置=線形・角度=最短弧） | 担当4（F-06） | **F-06 はこれをそのまま流用**（④ §4。補間計算は Engine が実装済み） |
| **一方通行デモ** | `web/sim_demo/record.mjs` → `replay.html/js` | 担当2/4 の参照実装 | 「sim → JSON → render」を W-10/W-11/F-06 がそのまま本番化 |
| **対戦マップ4枚** | `rsp` / `rsp_pillars` / `21x21_arena` / `fps_duel` | 担当2（W-14） | ③ §2-E ホワイトリスト表（`{id,name,mode,path}`） |
| **受入テスト/CI** | `make test`（sim 85検査）/ `.github/workflows/ci.yml` | 全員 | native/web/sim を壊す退行を毎 PR で検知。W-16 が FE 検査を追加 |
| **lint** | `make check`（13検査。layering 復旧済み） | 全員 | CR001〜015。common→モード依存も検知する |

---

## 3. 4人の役割

> 記号: 各担当の Issue は**依存順**に並べる。★=クリティカルパス（ゲート2 の芯）。

### 担当割当（2026-07-20）

| 担当 | 氏名 | 領域 | 既存作業の知識 |
|---|---|---|---|
| **担当2** | **samatsum** | ゲームサーバー（sim.wasm 駆動） | **★最重要**。sim API / snapshot / デモを作った本人 |
| 担当1 | torinoue | バックエンド基盤（Auth/REST/DB/DevOps） | 不要（標準的な Web バックエンド） |
| 担当3 | mamiyaza | フロント基盤（認証/ロビー/プロフィール） | 不要（標準的な React） |
| 担当4 | hminemur | フロントゲーム（GameView/HUD/統合） | 一部必要。samatsum とペアで進める |

- **samatsum → 担当2 は確定**。
  これまで全工程を1人で実装しており、`sim.wasm` を Node で駆動する W-10 の唯一の適任者で、snapshot 配信形（②§5-C）の設計元でもある。
  4つの中で**最も既存作業の知識を要するレーン**。
- **W-01（リポジトリ骨格）は samatsum が先行実施 → ✅完了**。
  npm workspaces で `backend`（Fastify+TS）/
  `frontend`（React+Vite+TS+Tailwind）/ `shared`（zod 契約）/ `infra` を追加し、
  両サーバの起動を実機検証済み（`npm install` → `npm run dev:backend` /
  `dev:frontend`）。
  **3人のブロッカーは解消済み**で、torinoue は W-02 から、mamiyaza は F-01 から、hminemur は F-06 準備から着手できる。
  samatsum は担当2（W-10〜）へ戻る。
- 担当4（hminemur）は render.wasm・補間の受信側で担当2 と密結合するため、
 **samatsum がペアレビューで支援**する（§2 の資産をそのまま配線する側）。
- 担当1・3・4 への torinoue / mamiyaza / hminemur の割当は**暫定（3人のスキル・希望で入れ替え自由）**。
  担当2 以外は既存 C の知識を前提としないため、誰が就いても着手できる。

### 担当1（torinoue）— バックエンド基盤（Auth / REST / DB / DevOps）

サーバーの「土台と正本データ」。REST 契約（③）と共有 zod スキーマの所有者。
**W-01（骨格）は samatsum が先行**するので、torinoue は完成した骨格の上で **W-02 から着手**する。

| 順 | Issue | 一言 |
|---|---|---|
| — | (W-01) | リポジトリ骨格。**samatsum が先行**（§3 冒頭）。torinoue は完成後に受け取る |
| 1 | W-02 | Fastify 起動（pino・zod 検証・③§1 エラーエンベロープ/レート制限） |
| 2 | W-03 | Prisma + SQLite スキーマ v1（③§3 の5テーブル）+ マイグレーション |
| 3 | W-04 | 認証一式（signup/login/logout/me・argon2id・Session Cookie） |
| 4 | W-05 | Origin 検証（REST 変更系 + WS アップグレード）と Cookie 認可の共通化 |
| 5 | W-15 | Docker Compose + nginx TLS + 単一コマンド起動。**評価要件「空clone→`docker compose up`」の芯**。継続タスク |
| 6 | W-13 | 試合永続化 + `match_result` 配信 + 履歴/統計 API（**担当2 の決着イベントを受けて DB 行を書く**） |
| 7 | W-14 | `GET /api/maps` + `welcome.map_text` 経路（③§2-E のホワイトリストを転記） |
| 8 | W-06 / W-07 | アバターアップロード / フレンド API（**5日では最初に落とす候補**。§5.1 の切り捨て順②） |

- **所有する契約**: ③ REST の全エンドポイント、`shared/` の zod スキーマ（担当3 と共同編集）。
- **最初の一手**: samatsum の W-01 着地を待って W-02（Fastify）→ W-03（Prisma）→ W-04（認証）の auth チェーン。
  W-15（docker）は継続タスクとして並行で進める。

### 担当2（samatsum）— ゲームサーバー（WS / GameRoom / sim.wasm 駆動）★

対人戦の心臓。**sim.wasm を Node 上で権威実行する唯一のレーン**。WS プロトコル（②）の所有者。
まず **W-01（骨格）を先に片付けて全員を解放**してから、本丸の W-10 に入る。

| 順 | Issue | 一言 |
|---|---|---|
| 0 | **W-01（先行）** | リポジトリ骨格（`backend/ frontend/ shared/ infra/`・TS ツール・`.env.example`・`.gitignore`）。全員のブロッカーのため最初に片付け、完成後は担当1（torinoue）へ渡す |
| 1 | **★W-10** | GameRoom + `sim.wasm` 統合（30Hz tick・偶数 tick 配信・状態機械）。**依存 E-11 は完了済みなので W-01 直後に着手可**。まず Node 単体で複数ルーム駆動を通す |
| 2 | ★W-08 | ロビー WS（presence/queue/room）。※auth（担当1 W-04）着地後に接続認可を差し込む |
| 3 | ★W-09 | マッチメイキング成立→GameRoom 生成→`match_found` |
| 4 | **★W-11** | ゲーム WS（join/input/leave/welcome/snapshot/event）。№5 snapshot<1KB / №6 不正メッセージ破棄 |
| 5 | ★W-12 | 切断/再接続/AI 代替（30秒猶予・`game_set_input_source`）。№4 |
| — | (W-13 連携) | 決着時に `match_end` を出し、永続化は担当1 へ渡す |

- **所有する契約**: ② WS プロトコル（§5 メッセージ・§6 GameRoom 状態機械）。
  **snapshot の JSON 形（②§5-C）を担当4 へ提示するのが最重要の同期点**。
- **最初の一手**: W-01 を終えたら `record.mjs` を出発点に W-10 を組む
  （`sim_create → game_add_combatant × 定員 → 
  毎tick sim_set_input → game_step → game_snapshot → JSON化`）。
  ENGINE_PHASE3_REPORT §W-10 申し送りの9項目が実装手順書。

### 担当3（mamiyaza）— フロント基盤（認証 / ロビー / プロフィール）

画面の土台と、ゲーム画面以外の全ページ。REST/ロビー WS の消費側。

| 順 | Issue | 一言 |
|---|---|---|
| 1 | **F-01** | 雛形（Vite/React/TS/Tailwind/Router/ErrorBoundary/StrictMode）。ビルド警告ゼロ |
| 2 | F-02 | fetch ラッパ + `shared` zod 接続（③§1 エラーエンベロープ・Toast） |
| 3 | F-03 | 認証画面 + ルートガード（担当1 W-04 と結合） |
| 4 | F-04 | 共通レイアウト + **Privacy/ToS 実文面**（プレースホルダー禁止） |
| 5 | ★F-05 | ロビー一式（5領域 + `useLobbySocket`）。**担当2 W-08・担当1 W-04 と結合** |
| 6 | F-09 | プロフィール/統計/履歴（担当1 W-13/W-06 と結合） |
| 7 | F-10 | フレンド UI（担当1 W-07 と結合） |
| 8 | F-11 | レスポンシブ + アクセシビリティ（375px・キーボードのみで対戦開始まで） |
| — | W-16 | CI に FE lint/型検査ジョブを追加（既存 `ci.yml` へ足すだけ） |
| — | (F-12 保1) | 観戦 UI。余力時のみ |

- **所有する契約**: ④ Frontend のうちゲーム画面以外。`shared/` zod を担当1 と共同編集。
- **最初の一手**: W-01 着地後すぐ F-01。F-02→F-03 は担当1 の W-02/W-04 と歩調を合わせる。

### 担当4（hminemur）— フロントゲーム（GameView / HUD / 統合）★

対戦画面。**§2 の render.wasm / snapshot_interp.js をそのまま配線する**レーン。ゲート2 の統合役。

| 順 | Issue | 一言 |
|---|---|---|
| 1 | **★F-06** | GameView 統合（render.wasm ロード・Canvas・**補間受け口**・入力送信）。**担当2 W-11 と結合**。`replay.html` が下敷き |
| 2 | ★F-07 | HUD オーバーレイ一式（8要素。grace→AI 遷移表示） |
| 3 | ★F-08 | マッチ遷移フロー（match_found→countdown→match_end モーダル→ロビー）。遷移中の WS 張り替えでコンソールエラーゼロ |
| — | (F-12 保1) | 観戦 HUD |

- **所有する契約**: ゲーム画面の描画・入力送信。
  **② §5-C の補間契約（now-100ms の2snapshotで補間・自 yaw はローカル優先）を担当2 と突き合わせる**。
- **最初の一手**: 着手前に `web/sim_demo/replay.html` を動かして
  「補間→`web_apply_snapshot`→`web_render_frame`」の流れを体で覚える。
  F-06 は snapshots.json を **WS 受信バッファに差し替えるだけ**。

---

## 4. 共有契約（4人の境界＝ここだけ合意すれば並行できる）

並行作業が破綻しないための「継ぎ目」。**各契約に1人の所有者を置き、他は消費者**とする。

| 契約 | 所有 | 消費 | 正本 |
|---|---|---|---|
| `shared/` zod スキーマ（型の単一情報源） | 担当1（+担当3 共同） | 全員 | ③ §1 / ② のペイロード。W-01 で骨格を置く |
| REST API（③） | 担当1 | 担当3 | REST_API_DESIGN |
| WS プロトコル（②§5 メッセージ / §6 状態機械） | 担当2 | 担当4 | WS_PROTOCOL_DESIGN。**snapshot JSON 形が最重要** |
| sim 公開 API（① §3-B） | （完了・不変） | 担当2 | `platform/sim.h` + PHASE3_REPORT §W-10 |
| snapshot 受信/補間（`web_apply_snapshot`・`snapshot_interp.js`） | （完了・不変） | 担当4 | ② §5-C |

**最優先の同期**: 担当2⇄担当4 の「snapshot JSON 形と補間契約」。ここが噛み合えば 2vs2 RSP が画面に出る。
次点は 担当1⇄担当3 の「REST エラーエンベロープと Cookie 認可」。

---

## 5. ゲート2までのクリティカルパス（4人の合流点）

ゲート2 = **2ブラウザで 2vs2 RSP が最後まで遊べる**。合流に必要な最小セット:

```
最初  samatsum: W-01（骨格）← 全員のブロッカー。まずこれを片付ける
        │
以降  torinoue: W-02 → W-03 → W-04（auth）  ＋ W-15（docker・継続）
      samatsum: W-10 → W-11 → W-08 → W-09   （game / lobby server）
      mamiyaza: F-01 → F-02 → F-03 → F-05    （lobby UI）
      hminemur:            F-06 → F-07 → F-08 （game UI）
                              │
                              ▼
                   ゲート2（2ブラウザで 2vs2 RSP 対戦成立）
```

- **W-10 は W-01 直後に着手可**（依存の E-11 は完了済み。auth を待たない）。
  samatsum は W-01 を終えたらすぐ W-10 の「Node 単体で sim.wasm が複数ルーム駆動する」ところまで先行できる＝最大の並行余地。
- W-08（ロビー WS）と F-05（ロビー UI）は auth（W-04）着地後。
- 4人の合流は **W-11（ゲーム WS）× F-06（GameView）** の1点。
  ここを両者が §4 の snapshot 契約で先に紙合わせしておくと、実装後の結合が速い。
- ゲート2 後の残り（W-13 永続化・F-09 統計・W-06/07 フレンド等）は Day 4 で各自のレーンに戻って消化（ゲート3）。

---

## 5.1 5日間の日割り（**現在有効な日程の正本**）

**期間は 5 日間です。**（当初は 2 週間で計画していました。⓪ §7 の 14 日表は当時の記録で、
日程としては無効です。）

残作業は Backend 15 件（W-02〜W-16）+ Frontend 12 件（F-01〜F-12）＝ 27 件を 4 人で 5 日。
**素直に並べると入りません。**そこで下表は「拒否条件と ゲート2 を最優先で確保し、
点数を積む機能は後ろに寄せて、入らなければ落とす」という組み方にしてあります。

| 日 | torinoue（BE基盤） | samatsum（ゲームサーバ）★ | mamiyaza（フロント基盤） | hminemur（フロントゲーム）★ |
|---|---|---|---|---|
| **1** | W-02 Fastify 起動 → W-03 Prisma スキーマ | **★W-10** GameRoom + `sim.wasm`（まず Node 単体で複数ルーム駆動） | F-01 雛形 → F-02 fetch ラッパ | `replay.html` を動かして理解 → F-06 着手（`snapshots.json` 相手に GameView の骨格） |
| **2** | W-04 認証一式（argon2id + Cookie） | **★W-11** ゲーム WS（join/input/snapshot/event） | F-03 認証画面 + ルートガード | ★F-06 継続（受信元を `snapshots.json` → WS バッファへ差し替え） |
| **3** | W-05 Origin 検証 → **W-15 docker 着手** | ★W-08 ロビー WS → ★W-09 マッチメイキング | ★F-05 ロビー一式 | ★F-07 HUD → ★F-08 マッチ遷移 |
| | | | | **▲ この日の終わりに ゲート2 判定**（2ブラウザで 2vs2 RSP が最後まで遊べる） |
| **4** | **W-15 完了**（空 clone → `docker compose up`）→ W-13 永続化 | W-12 切断/再接続/AI 代替 → W-14 maps API | F-04 **Privacy/ToS 実文面** → F-09 統計/履歴 | F-08 仕上げ → FPS モード結合（W-14 と） |
| **5** | **全員でハードニングと提出準備**（下記） | | | |

### Day 5（ハードニングと提出）の中身

⓪ §9.1 の評価台本に沿って、**全員で**潰します。ここは分担ではなく総力戦です。

| 項目 | 落とせない理由 |
|---|---|
| コンソールエラー/警告ゼロ（全画面・全遷移） | **未達は「プロジェクト拒否」** |
| Privacy/ToS が実データフローに即しているか | **未達は「プロジェクト拒否」** |
| HTTPS 通し（自己署名 CA 導入込み） | **必須要件** |
| 空フォルダ `git clone` → `docker compose up` の実測 | **必須要件**。W-15 の受入そのもの |
| SQLi/XSS の攻撃バッテリー・秘密情報スキャン（全履歴） | **必須要件** |
| F-11 レスポンシブ + キーボード操作 / W-16 CI に FE 検査 | 余力があれば |
| README（英語・第VI章の章立て）・ER図・AI 利用記録・デモ台本・リハーサル | 提出物 |

### 入らないときに落とすもの（この順で落とす）

上から順に切ります。**この4つは点数が減るだけで、拒否条件には触りません。**

1. **F-12 / W-12 の観戦（保1）** — 保険モジュール。最初に落とす
2. **W-06・W-07・F-10（アバター / フレンド）** — Minor モジュール。ロビーの presence だけ残す
3. **F-09 の統計ページ** — W-13 の永続化（DB 行の書き込み）まで通っていれば、表示は最小で可
4. **FPS モード（#10 Another game）** — ゲート2 が Day 3 に落ちた場合の緊急退避。
   ⓪ §7 のフォールバック規定どおり、保険モジュールへ差し替える

**逆に、絶対に落とせないもの**: Day 5 の表の上5行（拒否条件と必須要件）と、ゲート2。
点数モジュールを 1 つ諦めてでも、こちらを先に確保してください。

### この日割りの前提とリスク

- **1日 = 4人 × 1営業日**として組んでいます。1人でも丸1日欠けると Day 3 のゲート2 が崩れます。
- **最大のリスクは Day 2〜3 の samatsum ⇄ hminemur の結合**（W-11 × F-06）です。
  §4 の snapshot JSON 形を **Day 1 のうちに紙で合わせておく**と、Day 2 の実装が噛み合います。
  ここが1日ずれると全体が1日ずれます。
- **torinoue の W-04（認証）が Day 2 中に着地しないと**、mamiyaza の F-03/F-05 と
  samatsum の W-08 が同時に止まります。auth チェーンは最優先です。
- W-15（docker）は Day 3 に着手して Day 4 に終える形にしています。
  **Day 5 に回さないでください**。単一コマンド起動は必須要件で、詰まると丸一日溶けます。

---

## 6. 人間確認タスク（コードでは閉じない）

| 項目 | 誰 | 状態 |
|---|---|---|
| G-09 マップのテストプレイ承認 | 全員（代表 samatsum） | ✅ 済。テストプレイでプレイ感が不合格だったため samatsum がマップを改訂して再承認（**改訂版の repo への反映は samatsum が行う**） |
| CI 初回 green | オーナー（samatsum） | ✅ **達成**（2026-07-23、`908529d`）。`native` / `wasm` / `web-app` の3ジョブすべて success。billing ロック解除後も2つの実問題が残っていたが、いずれも特定・修正済み（下表）。**§7-2 の「green ならマージ可」運用はこれで有効** |

**CI で見つかった2つの問題（2026-07-23。いずれも原因特定・修正し green 達成）**

| ジョブ | 原因 | 修正 |
|---|---|---|
| `native` | **CI 環境に `libbsd-dev` が無かった**（W-01 以前から失敗）。minilibx のビルドは `test/mlx-test` も作り、Linux ではそのリンクが `-lbsd` を要求する（`codes/minilibx-linux/test/Makefile.mk`）。README の必要パッケージには記載があり、開発機には入っていたため再現しなかった | ワークフローへ `libbsd-dev` を追加 → **success 確認済み** |
| `wasm` | **W-01 が持ち込んだ回帰**。ルート `package.json` の `"type": "module"` により Node が `web/build/*.js`（emcc が生成する CommonJS の glue）を ESM と誤認し、`require()` の戻り値が関数でなくなる（`TypeError: createSim is not a function`）。W-01 以前は success だった | `web/package.json` に `{"type":"commonjs"}` を置いて当ディレクトリを CJS スコープに固定 → ローカルで `record.mjs`・`bench_render.mjs` の復旧を確認 |

> **教訓**: ルートに `"type": "module"` の `package.json` を置くと、
> リポジトリ内の既存 `.js`（emcc 生成物など）の解釈が変わる。
> TS モノレポを足すときは**既存の Node ツール（`web/sim_demo/record.mjs` 等）の再実行までを受入確認に含める**こと。
> W-01 の検証は backend/frontend の起動のみで、既存資産の回帰を見落とした。

---

## 7. 並行作業のルール（1人 → 4人で変わる点）

これまで1人だったので直接 main へ積んでこられたが、4人では衝突と退行を防ぐ運用に切り替える。

1. **ブランチ**: Issue 単位の feature ブランチ → PR。main へ直 push しない。
2. **CI を通す**: PR は
   `.github/workflows/ci.yml`（native/web/sim + `make check` + `make test` +
   xvfb smoke）が green で初めてマージ可。
   **C 側（Engine・Gameplay）を壊す変更は CI が止める**。
3. **契約変更は先に合意**: ②③ や `shared/` zod を変えるときは所有者（§4）に相談 → 設計書を先に改訂してから実装（各設計書の原則欄）。
4. **コミット規約**: CR021 に従い本文に Why を書く。**Co-Authored-By は付けない**。
5. **C 側は原則凍結**: Backend/Frontend の作業で C（`codes/`）を触る必要が出たら、それは設計の見落としの兆候。
   担当2（sim API 所有）に相談してからにする。
6. **共有の正本**: 進捗と Issue の受入は [BACKLOG.md](./5-バックログ.md)。本書は分担の地図。ズレたら両方直す。
